<div class="section">
    <h3 class="has-text-centered">Do you really want to delete this comment?</h3>
    <div class="has-text-centered">
        <form action="/challenges/{{challengeId}}/comments/{{commentId}}/delete" method="post">
            <input type="hidden" value="{{commentId}}" name="challengeId">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <input class="button is-danger" type="submit" value="Delete">
        </form>
        <a class="button" href="/challenges/{{challengeId}}/comments/{{commentId}}/preview">Cancel</a>       
    </div>
</div>